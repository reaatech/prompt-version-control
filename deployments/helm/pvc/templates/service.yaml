apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc.fullname" . }}
  labels:
    {{- include "pvc.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
